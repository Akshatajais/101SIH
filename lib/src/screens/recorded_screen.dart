import 'package:flutter/material.dart';

class RecordedScreen extends StatelessWidget {
  RecordedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Recorded Classes')),
      body: Center(
        child: Text('Recorded classes placeholder.'),
      ),
    );
  }
}


